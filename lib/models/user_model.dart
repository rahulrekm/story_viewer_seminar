class User {
  final String name;                // For showing the user image and name in every story
  final String profileImage;

  const User({
    required this.name,
    required this.profileImage,
});
}

